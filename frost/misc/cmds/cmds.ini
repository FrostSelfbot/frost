[FROST ON TOP]

reboot // Restarts the selfbot.
frostembed // Sends the frost website embed.
cmdlistprint // Prints all of the Frost commands without blank lines or anything else.

Fun commands:
lenny // Sends a lenny face.
eightball <question> // Ask the 8ball a question.
suntzu // Sends a Sun Tzu quote.
floodchat // Floods the chat with invisible unicodes.
ben // Sends a ben gif.
hack <user> // Hacks the given user.
slap <user> // Slaps the given user.
hug <user> // Hugs the given user.
kiss <user> // Kisses the given user. xoxo
panda // Sends a panda image.
dog // Sends a dog image.
cat // Sends a cat image.
fox // Sends a fox image.
birb // Sends a bird image.
coinflip // Coinflip.
ghostping <user> // Ghost pings an user.
ascii <text> // Convert text into ASCII.
chucknorris // Sends a Chuck Norris joke.
lmgtfy <text> // Searches on LMGTFY.
dox <user> // "Doxxes" an user
randombypassedslur // Sends a random bypassed slur
emojiflood // Spams emojis.
bypass <n word, gay slur, trans slur, r word> // Bypasses the given slurs with unicode characters. ("n word", "gay slur", "trans slur", "r word")
gayrate // Shows how gay an user is.
retardrate // Shows how retarded an user is.

Utility commands:
help // Sends this message.
spam <delay, interval, text> // Spams the given text.
generateidentity // Generates an identity.
open <path to program or supported name> // Opens the given program.
openhelp // Shows supported programs.
ping <host> // Pings the given host.
googlemaps <address> // Opens the given address with google maps.
setnickname <text> // Sets your nickname.
invisnick // Makes your nickname invisible.
afk <on/off> // Toggles AFK Mode.
stealpfp <user> // Steals an users profile picture and saves it.
serverpfp // Sends the server pfp.
hypesquad <house> // Sets your HypeSquad House.
serverinfo // Shows current server info.
whois <discord user> // Shows info about user.
clear <number of messages> // Deletes the given number of messages.
price <crypto> // Only supports ethereum and bitcoin.
cloneserver <id> // Make a server and type the command in it.
executebrainfuck <code> // Executes brainfuck code.
saveprofile // Saves your profile.
fakename // Sends a fake name.
leaveserver // Leaves the given server.
coronastats // Displays coronavirus stats

Codeblocks:
lua
js
java
cpp
cs
ini
py

Encoding commands:
base64 <text> // Encodes given text into Base64.
md5 <text> // Encodes given text into MD5.
sha512 <text> // Encodes given text into SHA-512.
sha224 <text> // Encodes given text into SHA-224.
sha384 <text> // Encodes given text into SHA-384
uwu <text> // Converts text into uwus.
tobrainfuck <code> // Converts code into brainfuck.
hacker <text> // Converts text into 1337 HACKS.
zalgo <text> // Encodes given text into Zalgo.


NSFW:
hentai // Sends a hentai gif.
boobs // Sends boobs images.

Administration commands:
banuser <user> // Bans the given user.
kickuser <user> // Kicks the given user.
textchannel <name> // Creates a text channel with the given name.
voicechannel <name> // Creates a voice channel with the given name.
category <name> // Creates a category with the given name.

Abusive commands (not safe, very risky.):
kickall // Kicks everyone.
banall // Bans everyone.
dmall <text> // DMs everyone from the server with a 15 second delay.

Activity commands:
playing <name> // Sets your activity to playing.
listening <name> // Sets your activity to listening.
watching <name> // Sets your activity to watching.
streaming <name> // Sets your activity to streaming.
stopactivity // Stops your activity.

Animated commands:
memz // MEMZ!!
fuckyou // Fuck you.
bomb // BOOM!
suckmy // Suck my dick

Copypastas:
masturbate
discorddown
havefunkiddo
justgonnasayit
edate
inactive
bubblewrap
yourmotherandi
senpai
discordmodgoodbye
zerotwo // O_____o
rickroll

[FROST ON TOP]